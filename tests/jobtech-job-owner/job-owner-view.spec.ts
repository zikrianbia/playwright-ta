import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * JobTech Job Owner (Keith) — Job Owner View
 * Views assigned jobs in JobTech workspace.
 */
test.describe('JobTech Job Owner (Keith) - Job Owner View', () => {

    test('can see jobs assigned as Job Owner', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can view job details for assigned jobs', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('has limited edit permissions on job details', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
