import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * E2I Superadmin (Grace e2i) — Permissions
 * E2I workspace visibility & field access checks.
 */
test.describe('E2I Superadmin - Permissions', () => {

    test('can see all E2I workspace jobs', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can hire on behalf of a partner employer', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('job detail shows About Job, Jobseeker Matches, and Applicants tabs', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can invite jobseekers from E2I talent pool', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can shortlist or reject applicants', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
