import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * E2I Superadmin (Grace e2i) — Jobseeker Matching
 * Matches & invites jobseekers from the E2I talent pool.
 */
test.describe('E2I Superadmin - Jobseeker Matching', () => {

    test('Jobseeker Matches tab is visible on job detail', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('matched E2I jobseekers are displayed', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('each jobseeker shows name, email, match score, and badges', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can open a jobseeker profile (CV, skill matches, skill gaps)', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can send an invitation to a jobseeker', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('invitation is sent successfully', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
