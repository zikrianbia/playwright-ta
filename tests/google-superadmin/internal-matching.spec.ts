import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * Google Superadmin (Super admin TA) — Internal Matching
 * Matches internal employees for TM jobs.
 */
test.describe('Google Superadmin - Internal Matching', () => {

    test('Internal Matches tab is visible', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('matched internal employees are displayed', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('each internal match shows name, email, and match score', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can send invitation to an internal employee', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('invitation is sent successfully', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
