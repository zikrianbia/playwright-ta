import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * Google Superadmin (Super admin TA) — Applicant Management
 * Reviews and finalizes applicants from internal and external sources.
 */
test.describe('Google Superadmin - Applicant Management', () => {

    test('applicants appear in the Applicants tab', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('application status shows "Applied"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('each applicant is labeled as Internal or External', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can change status to "Keep In View"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can finalize status as "Successful"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('hiring decision reflected in Applicants tab', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
