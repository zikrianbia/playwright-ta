import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * Google Superadmin (Super admin TA) — External Matching
 * External talent unlock and matching.
 */
test.describe('Google Superadmin - External Matching', () => {

    test('External Matches tab is visible', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Unlock Talents option is displayed when external talents available', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can unlock external talents by completing payment', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('external talent matches displayed after unlock', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('each external match shows name, email, and match score', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can send invitation to an external talent', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
