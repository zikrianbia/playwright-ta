import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * Partner Admin (Shannon Stone) — Test Hiring Enterprise 2 — Job Creation
 * Partner posts jobs that require E2I approval.
 */
test.describe('Partner Admin - Job Creation', () => {

    test('Employer field is pre-filled and disabled', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Hiring Manager field is pre-filled and disabled', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Job Owner field is pre-filled as E2I Job Owner and disabled', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('only E2I channel is selectable', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Internal Employees and External Talents channels are disabled', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('job is created in "Pending Approval" status', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('pending job shows approval banner', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('only job description is visible while pending', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
