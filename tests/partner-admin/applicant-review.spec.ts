import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * Partner Admin (Shannon Stone) — Test Hiring Enterprise 2 — Applicant Review
 * Reviews shortlisted applicants from E2I and finalizes hiring outcome.
 */
test.describe('Partner Admin - Applicant Review', () => {

    test('sees no applicants before E2I shortlists', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('shortlisted applicant appears in Applicants tab', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('applicant status shows "Shortlisted"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can review applicant details', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can change status to "Keep In View" — takes ownership', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('status becomes read-only for E2I after partner takes ownership', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can finalize outcome as "Successful"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('can finalize outcome as "Unsuccessful"', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('final status reflected in both E2I and Partner workspaces', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
