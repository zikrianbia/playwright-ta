import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * 1B. Test Hiring Enterprise 2 (Partner only) - Workspace Visibility & Permissions
 */

test.describe('1B. Partner Employer - Permissions', () => {

    test('Partner employer cannot edit ownership fields', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner employer can only publish to E2I channel', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Job submission requires E2I approval', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner sees pending approval banner, no tabs accessible', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner can only view job description while pending', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner sees rejection message and reason', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Approved job visible to E2I and partner', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner sees restricted job tabs after approval', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner sees no applicants before E2I shortlist', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Shortlisted applicant appears for partner', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner takes ownership after Keep In View', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Partner can finalize applicant outcome', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
