import { test, expect } from '../../fixtures/test-base';
import { ACCOUNTS } from '../../test-data/accounts';

/**
 * 1C. Google (TM only) - Workspace Visibility & Permissions
 */

test.describe('1C. Google (TM Only) - Permissions', () => {

    test('TM-only company sees only its own workspace jobs', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Job posting ownership fields are locked', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('E2I channel disabled, Internal and External enabled', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Can select multiple allowed channels', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Job detail tabs visibility correct for TM-only job', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('Internal Match shown when Internal channel selected', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });

    test('External Match visible after unlocking', async ({ page }) => {
        test.skip(true, 'To be implemented');
    });
});
